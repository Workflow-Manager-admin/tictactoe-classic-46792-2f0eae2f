/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};=/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};q/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};;/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};H/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};W/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};k/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};P/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};=/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};q/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};w/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};k/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};;/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};=/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};=/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};=/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};>/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};y/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};_/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};_/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};f/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};\/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};|/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};$/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};_/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};	/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};@/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};@/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};E/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};D/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};I/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};T/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};_/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};M/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};O/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};D/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};E/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};?/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};\/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};$/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};y/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};-/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};\/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};|/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};|/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};f/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};|/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};$/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};y/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};j/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};S/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};3/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};0/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};0/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};0/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};[/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};w/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};H/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};W/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};k/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};P/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};g/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};(/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};b/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};//**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};x/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};h/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};P/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};a/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};s/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};{/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};P/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};U/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};B/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};L/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};I/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};C/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};_/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};U/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};R/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};L/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};]/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};./**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};?/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};r/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};u/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};c/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};i/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};:/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
}; /**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};d/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};v/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};l/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};o/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};p/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};m/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};e/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};n/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};t/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};'/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};,/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};}/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};)/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};;/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};
/**
 * Custom Webpack config patch for TicTacToe app:
 * - Resolves 'Invalid Host header' by allowing all hosts through devServer config.
 */
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  // ...your existing configuration (assume merged with what's in use)...
  devServer: {
    // Allow all hosts to prevent 'Invalid Host header' error when accessed via IP or non-default hostname
    allowedHosts: 'all',    // Accept all Host headers (for dev only)
    host: '0.0.0.0',        // Listen on all local network interfaces (useful for LAN/dev VM)
    // If you previously set 'public', you can remove or adapt it as needed
    hot: true,
    // ...add or merge any other required devServer options here...
  },
  // ...rest of config...
};